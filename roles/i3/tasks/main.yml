---
- name: Install i3
  package:
    name:
    - i3
    state: present
  become: yes

- name: Ensure i3 config dir is present
  file:
    path: '{{ i3_config_dir }}'
    state: directory
    recurse: true

- name: Configure i3
  template:
    src: config.j2
    dest: '{{ i3_config_location }}'